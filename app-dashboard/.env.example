# No env vars are required.
# The dashboard uses the mobile app's public Firebase web config, which is
# hardcoded in src/lib/firebase.js (safe to expose — security is enforced by
# Firestore rules). Nothing secret lives in this build.
